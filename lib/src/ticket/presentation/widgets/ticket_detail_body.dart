import 'package:flutter/material.dart';

class TicketDetailBody extends StatelessWidget {
  const TicketDetailBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
